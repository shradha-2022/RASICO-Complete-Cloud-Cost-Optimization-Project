services:
  - type: web
    name: rasico-complete-cloud-cost-optimization
    runtime: python
    pythonVersion: 3.11.8  # Add this line
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    # ... rest of your config
